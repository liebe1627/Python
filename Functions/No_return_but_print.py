# If the function reaches the end and there is no return function present then as no value goes back to the caller
# Default value of return with just print is sent as None

def Check():
    print("Hello")
c = Check()
print(c)